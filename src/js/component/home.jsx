import React from "react";
import { Box } from "./box"

const Home = () => {
	return (
		<React.Fragment>
		<main className="container">
		<Box/>
		</main>
		</React.Fragment>
	);
};

export default Home;
